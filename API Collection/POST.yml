info:
  name: POST
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:8899/sapapi
  body:
    type: form-urlencoded
    data:
      - name: bse_id
        value: "1"
      - name: sap_terminnummer
        value: "31114"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
